// @ts-check

/** @type {import('prettier').Config} */
const config = {
  singleQuote: true,
  trailingComma: 'none',
  arrowParens: 'always',
  semi: false,
  printWidth: 100,
  tabWidth: 2
}

export default config
